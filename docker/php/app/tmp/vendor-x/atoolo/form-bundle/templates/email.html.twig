<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">

<html lang="{{ lang }}">
<head>
    <title>{{ tenant.name }} – Webform</title>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        {{ include('@AtooloForm/email.html.styles.css') }}
    </style>
</head>
<body>
<div>
    <div class="header">
        <span class="title">{{ 'email.header'|trans({'{url}': url}, 'form')|raw }}</span>
    </div>

    <div class="container">
        <h1 class="header">{{ 'email.headline'|trans({}, 'form') }}</h1>
        {{ include('@AtooloForm/email.html.summary.twig') }}
    </div>

    <div class="footer">
        {{ 'email.footer'|trans({'{host}': host, '{date}': date|date('d.m.Y'), '{time}': date|date('H:i:s')}, 'form')|raw }}
    </div>
</div>
</body>
</html>